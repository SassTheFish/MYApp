<nav class="navbar navbar-expand-lg navbar-dark navy">
    

  <div class="container-fluid">  

      <a class="navbar-brand" href="/"><img src="/images/SwimmerLower.png" alt="logo" style="width: 50px; margin-right: 10px; padding:0 ;">MySwimming</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>   
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">

          <li class="nav-item">
            <a class="nav-link <% if(path === "/minapage") { %>
              highlightNav
            <% } %>" href="/minapage">Mina</a>
          </li>

          <li class="nav-item">
            <a class="nav-link <% if(path === "/harjutused") { %>
              highlightNav
            <% } %>" href="/harjutused">Harjutused</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <% if(path === "/ukekavad" | path === "/ujumisekavad") { %>
              highlightNav
            <% } %>" href="/ukekavad">Kavad</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <% if(path === "/treening-gruppid") { %>
              highlightNav
            <% } %>" href="/treening-gruppid">Treening gruppid</a>
          </li>
        </ul>
        <% if (currentUser) { %>
          <form id="navform" action="/logout" method="POST">
            <label id="logbutton">
              <svg xmlns="http://www.w3.org/2000/svg" width="23" height="23" fill="currentColor" class="bi bi-person-square" viewBox="0 0 16 16">
                <path d="M11 6a3 3 0 1 1-6 0 3 3 0 0 1 6 0z"/>
                <path d="M2 0a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V2a2 2 0 0 0-2-2H2zm12 1a1 1 0 0 1 1 1v12a1 1 0 0 1-1 1v-1c0-1-1-4-6-4s-6 3-6 4v1a1 1 0 0 1-1-1V2a1 1 0 0 1 1-1h12z"/>
              </svg>
              <%= currentUser.username %> 
            </label>
            <button class="Out" type="submit">Logout</button>
          </form>
        <% } else { %>
          <form id="navform" action="/login" method="GET">
            <button class="In w-100" type="submit">Login</button>
          </form>
        <% } %> 
        
      </div>
      
    </div>
  </nav>