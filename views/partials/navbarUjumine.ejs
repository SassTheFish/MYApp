<nav class="navbar navbar-expand-lg navbar-dark ujuminenav">
    

  <div class="container-fluid">  

      <a class="navbar-brand" href="/"><img src="/images/SwimmerLower.png" alt="logo" style="width: 50px; margin-right: 10px; padding:0 ;">MySwimming</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button> 
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">

          <li class="nav-item">
            <a class="nav-link <% if(path === "/minapage") { %>
              highlightNav
            <% } %>" href="/minapage">Mina</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <% if(path === "/harjutused") { %>
              highlightNav
            <% } %>" href="/harjutused">Harjutused</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <% if(path === "/ukekavad" | path === "/ujumisekavad") { %>
              highlightNav
            <% } %>" href="/ukekavad">Kavad</a>
          </li>
          <% if (currentUser) { %>
            <form id="navform" action="/logout" method="POST">
              <button class="Out" type="submit">Logout</button>
            </form>
          <% } else { %>
            <form id="navform"  action="/login" method="GET">
              <button class="In" type="submit">Login</button>
            </form>
          <% } %>
          
        </ul>
      </div>
    </div>
  </nav>